import React, { useState } from "react";
import { fetchMemes } from "../actions/fetchMemes";

const SearchMeme = (props) => {
    return (
        <div>
        <br /> 
        <form>
            {/* <input type="text" id="header-search" placeholder="search memes" onChange={(event)=>props.callback(event.target.value)}/> */}
            
        </form>
        <br />
        <br />

        </div>
        
    )
}

export default SearchMeme;